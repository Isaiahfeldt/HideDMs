# HideDMs (Vencord Plugin)

**Hide specific DM threads** via right‑click. Hidden DMs are visually removed from the DM list until revealed or unhidden.

## Features
- Right‑click any DM → **Hide/Unhide**
- **Persistent** hidden list across restarts
- **Secret reveal** hotkey (Ctrl+Shift+<Key>, default `K`) to temporarily show all hidden DMs (configurable in plugin settings)

## Roadmap / To-Do
- Suppress desktop/push notifications for hidden DMs
- Mute notification sounds for hidden DMs